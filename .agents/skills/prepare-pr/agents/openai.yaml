interface:
  display_name: "Prepare PR"
  short_description: "Objectively summarize changes and prepare pull requests"
  default_prompt: "Use $prepare-pr to objectively summarize the selected Git changes and prepare a pull request draft."
